# Session Handoff (2026-03-01C)

## 本次目标
- 在现有远控项目中增加“电脑系统声音传输”，要求音画同步、尽量低性能开销、稳定可靠。

## 本次方案做了什么
- 后端：
  - 基于 `aiortc` 增加系统音频采集与 WebRTC 音轨发送（`SystemAudioPump` / `SystemAudioTrack`）。
  - WebRTC 协商改为音视频同一 `RTCPeerConnection`。
  - 增加音频状态上报、开关、设备切换接口。
  - 增加 SDP 音频参数（Opus）。
  - 补了大量 WebRTC 调试日志（offer/answer、transceiver、state、客户端回传日志）。
- 前端：
  - 增加音频元素、音频状态 UI、Unlock 逻辑、音频统计显示。
  - WebRTC 连接流程增加状态上报与错误可见化。
  - 增加强制缓存版本号，避免旧 JS/CSS 干扰。
- 启动与依赖：
  - `requirements.txt` 增加 `soundcard`。
  - `start.bat` 不再默认强制 `--dxgi`。

## 已经确认的关键结论
- WebRTC 建链“已成功”：
  - 日志明确出现 `dir=[sendonly,sendonly]`。
  - 客户端日志有 `ontrack video/audio`。
  - 服务端日志有 `video recv started`，并持续计数（到 300/600/900）。
- 说明问题已经不在“协商失败”，而是“运行稳定性与音频可听性”。

## 现状问题（未解决）
- 画面：
  - 用户主观感受明显变糊（较历史方案差）。
  - 会出现卡住或中途回落 MJPEG 的体验。
- 声音：
  - 平板端仍“无可听声音”。
  - `soundcard` 出现 `data discontinuity in recording` 警告。
- 设备副作用：
  - 连接蓝牙耳机时，启动程序会影响电脑本地蓝牙音频状态（疑似设备回环/配置切换相关）。

## 为什么用户感觉“方案不太像”
- 本次在同一轮里叠加了较多实验性改动（协商、日志、回退策略、编码参数、采集策略），可观测性提高了，但行为复杂度也显著提高。
- 对用户而言，结果是“改了很多但体验变差”，不符合“成熟可靠、低折腾”的预期。

## 当前工作区改动文件（待你回退）
- `src/remote_control/server_app.py`
- `static/app.js`
- `static/style.css`
- `templates/index.html`
- `start.bat`

## 建议回退策略（推荐）
1. 先整体回退到本轮开始前的稳定版本。
2. 重新走“最小增量”路线，只做两步：
   - 第一步：只恢复稳定视频链路（不加音频、不过多日志扩展）。
   - 第二步：只接入单一已知稳定音频设备（优先 VB-CABLE，避免蓝牙路径），并固定参数，不做复杂自动切换。
3. 每一步都要求“可连续运行 30 分钟”再进入下一步。

## 下次继续时的最小原则
- 一次只改一个变量（例如只改设备选择，不改编码参数）。
- 先保稳定，再提画质与功能。
- 禁止同时引入多种回退路径（避免状态机复杂化）。
